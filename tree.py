import node


class tree:

    def __init__(self, head, name='tree'):
        self.name = name
        self.head = head

    # def printTree(self,head):
    #     return [head.value] + [self.printTree(head.childs)]
